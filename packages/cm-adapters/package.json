{
  "name": "@conductor/cm-adapters",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@conductor/cm-core": "workspace:*",
    "@conductor/github": "workspace:*",
    "execa": "^9.6.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@conductor/tsconfig": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
